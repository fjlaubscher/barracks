{
  "name": "barracks",
  "private": true,
  "workspaces": [
    "barracks-api",
    "barracks-worker",
    "barracks-web"
  ],
  "scripts": {
    "build": "yarn workspace barracks-web build",
    "start": "yarn workspace barracks-web start",
    "start:api": "yarn workspace barracks-api start",
    "format": "yarn workspaces run format",
    "test": "yarn workspace barracks-web test"
  },
  "resolutions": {
    "puppeteer": "^13"
  },
  "devDependencies": {
    "prettier": "^3.0.1"
  }
}
